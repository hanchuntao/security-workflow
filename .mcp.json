{
  "author": "Chuntao Han",
  "mcpServers": {
    "security-workflow-mcp-engine": {
      "command": "python",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp_entry.py"],
      "env": {
        "ENV": "${SECURITY_WORKFLOW_ENV:-production}"
      }
    }
  }
}
